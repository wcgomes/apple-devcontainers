# Apple Dev Container CLI (adevcontainer) — agent bootstrap

## Start here

- **Knowledge** → load `wiki-query` skill. Do not invent conventions; look them up.
- **Codebase** → CodeGraph (when available): MCP `codegraph_explore` or CLI `codegraph explore` before broad grep/read.